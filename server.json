{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.legumeinfo/legumista",
  "title": "Legumista",
  "description": "Legume genomics over MCP: the LIS Data Store catalog, InterMine queries, scholarly search, NCBI/EDirect, and pysam tools.",
  "repository": {
    "url": "https://github.com/legumeinfo/legumista",
    "source": "github"
  },
  "websiteUrl": "https://github.com/legumeinfo/legumista",
  "version": "0.1.4",
  "packages": [
    {
      "registryType": "pypi",
      "registryBaseUrl": "https://pypi.org",
      "identifier": "legumista",
      "version": "0.1.4",
      "runtimeHint": "uvx",
      "transport": {
        "type": "stdio"
      },
      "packageArguments": [
        {
          "type": "positional",
          "value": "mcp",
          "valueHint": "subcommand",
          "description": "Start the MCP server (stdio) via `uvx legumista mcp`. Append '--allow-write' to permit write operations."
        }
      ],
      "environmentVariables": [
        {
          "name": "LEGUMISTA_CATALOG_URL",
          "description": "Where to download the LIS catalog from on startup. Defaults to the published LIS-autocontent release asset.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "LEGUMISTA_CACHE_DIR",
          "description": "Where the downloaded catalog is cached. Defaults to ~/.cache/legumista. A warm cache is what the server falls back to when the catalog host is unreachable.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "LEGUMISTA_CATALOG_POLL",
          "description": "Seconds between background catalog checks (conditional GET, so an unchanged catalog costs a 304). Default 86400; 0 disables.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "LEGUMISTA_WEBHOOK_SECRET",
          "description": "Shared secret enabling POST /catalog/refresh, authenticated with GitHub's X-Hub-Signature-256 HMAC scheme. Unset, the route is not registered at all. HTTP transport only.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "LEGUMISTA_HOME",
          "description": "Directory the genomics tools may read local files from (they are sandboxed to it). Defaults to the working directory; also settable with `legumista mcp -C DIR`.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "LEGUMISTA_PYSAM_ALLOWED_URLS",
          "description": "Optional comma-separated http(s) URL-prefix allowlist for the genomics tools; when unset, any public host is allowed (private/loopback are always blocked).",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
